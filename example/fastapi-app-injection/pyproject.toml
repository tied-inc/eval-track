[project]
name = "fastapi-app-injection"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "tracker",
    "uvicorn>=0.34.0",
]

[tool.uv.sources]
tracker = { path = "../../tracker", editable = true }
